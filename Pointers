# include <stdio.h>

int main() {
    int i = 8;
    printf("Value of i : %d and its address: %u", i, &i);
    
    int *ptr_i;
    ptr_i = &i;
    printf("\nValue of ptr_i : %u and value pointed by the pointer is : %d", ptr_i, *ptr_i); 

    return 0;
}
Value of i : 8 and its address: 1213627044
Value of ptr_i : 1213627044 and value pointed by the pointer is : 8

=== Code Execution Successful ===
